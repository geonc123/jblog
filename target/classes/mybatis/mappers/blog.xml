<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="blog">
<select id="selectByUserName" parameterType="String" resultType="com.javaex.vo.BlogVo">
	select	b.id as id, 
			b.blogtitle as blogtitle, 
			b.logofile as logofile 
	from blog b 
	where b.id=#{id}
</select>


<update id="update" parameterType="com.javaex.vo.BlogVo">
	<![CDATA[
	update blog SET
        blogtitle = #{blogTitle},
        logofile = #{logoFile}
        where id = #{id}
	
]]>
</update>


<update id="insertBlog" parameterType="String">
	<![CDATA[

		insert into blog (id , blogtitle)
        			values (#{id}, #{id})
	]]>
</update>

</mapper>