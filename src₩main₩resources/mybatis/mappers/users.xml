<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="users">

<insert id="insert" parameterType="com.javaex.vo.UsersVo">
<![CDATA[

	insert into users 
   	 values  (
            	seq_users_no.nextval,
            	#{userName},
            	#{password},
     	 		sysdate,
           		#{id}
        	 )


]]></insert>

<select id="selectById" parameterType="String" resultType="com.javaex.vo.UsersVo">

<![CDATA[

select  userno, 
		username, 
		password, 
		joindate , 
		id 
from users
where id = #{id}

]]>
</select>

<select id="selectByIdAndPassword" parameterType="com.javaex.vo.UsersVo" resultType="com.javaex.vo.UsersVo">
<![CDATA[
	select  userno, 
			username, 
			password, 
			joindate , 
			id 
	from users 
	where id = #{id} and password= #{password}
]]>
</select>


</mapper>